package t.macbeth.cse310androidworkshop2solved

/* Enumeration representing each of the pieces.  A string
   version (text) of each enum is provided to support
   display and saving as needed.
 */
enum class Piece(val text: String) {
    X("X"), O("O"), EMPTY("")
}